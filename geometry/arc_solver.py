#solving for an arc requires at a minimum:
# one point, one bearing, radius and delta, direction
# two points, one opp length, dir, compute bearing and solve for radius / delta
# three points, compute everything else
#
# calc vectors from coordiantes first
# calc formulas for lengths / delta